# Core — 100% local, sin API keys.
# Con techo de versión: una major nueva puede romper el server o introducir
# cambios no auditados. Subilo a conciencia, no automáticamente.
mcp>=1.0.0,<2.0.0
pydantic>=2.0.0,<3.0.0

# Opcional — ranking semántico local para smart_read (si no está, se usa BM25 puro-Python):
# Nota: requiere onnxruntime; verificar compatibilidad con tu versión de Python.
# pip install fastembed

# Desarrollo — suite de tests (tests/test_router.py):
# pip install pytest
# pytest>=8.0.0

# Auditoría de dependencias (recomendado antes de actualizar):
# pip install pip-audit && pip-audit
